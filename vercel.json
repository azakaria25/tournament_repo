{
  "buildCommand": "CI=false npm run install:all && cd frontend && CI=false npm run build",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/node",
      "config": {
        "nodeVersion": "18.x"
      }
    }
  ]
} 